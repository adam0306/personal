---
version: "2.3"
services:
  emby:
    image: emby/embyserver
    container_name: embyserver
    environment:
      - UID=1000
      - GID=1000
    volumes:
      - /home/adam/config/emby/:/config
      - /mnt/movies/:/mnt/movies
      - /mnt/shows/:/mnt/shows
    ports:
      - 8096:8096
    devices:
      - /dev/dri:/dev/dri
    restart: unless-stopped
  embystat:
    image: lscr.io/linuxserver/embystat:latest
    container_name: embystat
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/CST
    volumes:
      - /home/adam/config/embystat/:/config
    ports:
      - 6555:6555
    restart: unless-stopped
  prowlarr:
    image: lscr.io/linuxserver/prowlarr:latest
    container_name: prowlarr
    environment:
      - PUID=0
      - PGID=0
      - TZ=Etc/CST
    volumes:
      - /home/adam/config/prowlarr:/config
    ports:
      - 9696:9696
    restart: unless-stopped
  sonarr:
    image: linuxserver/sonarr:4.0.1
    container_name: sonarr
    restart: unless-stopped
    environment:
      - TZ=Etc/CST
      - PUID=0
      - PGID=0
    volumes:
      - /mnt/shows:/data
      - /home/adam/config/sonarr:/config
      - /mnt/config/transmission/downloads:/downloads
    ports:
      - 8989:8989/tcp
  readarr:
    image: lscr.io/linuxserver/readarr:develop
    container_name: readarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=ETC/CST
    volumes:
      - /home/adam/config/readarr:/config
      - /mnt/books:/books #optional
      - /mnt/config/transmission/downloads::/downloads #optional
    ports:
      - 8787:8787
    restart: unless-stopped
  radarr:
    image: linuxserver/radarr:5.2.6
    container_name: radarr
    restart: unless-stopped
    environment:
      - TZ=Etc/CST
      - PUID=0
      - PGID=0
    volumes:
      - /mnt/movies:/movies
      - /mnt/config/transmission/downloads:/downloads
      - /home/adam/config/radarr:/config
    ports:
      - 7878:7878/tcp
  transmission:
    image: lscr.io/linuxserver/transmission:latest
    container_name: transmission
    environment:
      - PUID=0
      - PGID=0
      - TZ=Etc/CST
    volumes:
      - /home/adam/config/transmission:/config
      - /mnt/config/transmission/downloads:/downloads
      - /mnt/config/transmission/watch/folder:/watch
    ports:
      - 9091:9091
      - 51413:51413
      - 51413:51413/udp
    restart: unless-stopped
  semaphore:
    ports:
      - 3000:3000
    image: semaphoreui/semaphore:latest
    environment:
      SEMAPHORE_DB_DIALECT: bolt
      SEMAPHORE_ADMIN_PASSWORD: changeme
      SEMAPHORE_ADMIN_NAME: admin
      SEMAPHORE_ADMIN_EMAIL: admin@localhost
      SEMAPHORE_ADMIN: admin
  # ntopng:
  #   image: ntop/ntopng:stable
  #   restart: always
  #   network_mode: "host"
  #   volumes:
  #     - /etc/ntopng.license:/etc/ntopng.license:ro
  #   command: ['--interface', 'tcp://*:5556c', '-F', 'clickhouse', '--disable-login'] # , '--insecure']
